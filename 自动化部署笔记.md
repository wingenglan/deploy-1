# 自动化部署学习笔记

> 本笔记记录两种自动化部署方式的完整学习过程：
> 1. **GitHub Actions** —— push 代码后云端自动构建部署（已完成 ✅）
> 2. **Jenkins** —— 可指定分支、指定项目的手动/自动部署（学习中 🚧，学完补充剩余部分）
>
> 项目说明：`deploy-1` 是一个前后端分离的"Flowboard 任务看板"示例项目
> - `frontend/`：Vue 3 + TypeScript，构建产物由 Nginx 托管
> - `backend/`：Go REST API，SQLite 持久化
> - `docker-compose.yml`：生产运行编排（前端 Nginx 反代后端 API）

---

# 第一部分：GitHub Actions 自动部署 ✅

## 1. 原理

```
你在本地 push 代码到 GitHub main 分支
        │
        ▼
GitHub 云端检测到 push → 在云端机器上运行 .github/workflows/deploy.yml
        │
        ▼
在云端构建（跑 go test、npm build，验证代码没问题）
        │
        ▼
通过 SSH 连到你的服务器 → git 拉最新代码 → 执行 scripts/deploy.sh
        │
        ▼
服务器上用 docker compose 重新构建镜像并重启容器（部署完成）
```

**关键理解：构建过程跑在 GitHub 的云端，你的电脑和服务器都只是被通知的角色。**

## 2. 涉及的文件

| 文件 | 作用 |
|---|---|
| `.github/workflows/deploy.yml` | 部署的"剧本"：定义什么时候触发、做什么 |
| `scripts/deploy.sh` | 服务器上真正执行的部署命令 |
| `docker-compose.yml` | 定义前后端如何以容器方式跑起来 |

> ⚠️ 学习完成后我把 `.github` 改名成了 `.github-1`，让它不再生效（避免和 Jenkins 重复触发部署）。需要恢复时改回 `.github` 即可。

## 3. 触发条件

```yaml
on:
  push:
    branches: [main]
```

只有 **push 到 main 分支** 才触发部署；推其他分支不触发。

## 4. 完整步骤（首次配置）

### 4.1 准备工作

1. 把 `deploy-1` 作为独立仓库推送到 GitHub，默认分支 `main`。
2. 服务器上安装 Docker 与 Docker Compose Plugin，把仓库克隆到 `/opt/flowboard`。
3. 服务器上的部署用户需要有运行 Docker 的权限。

### 4.2 在 GitHub 仓库配置密钥（Secrets）

位置：仓库 **Settings → Secrets and variables → Actions → New repository secret**

| Secret 名 | 填什么 |
|---|---|
| `DEPLOY_HOST` | 服务器 IP 或域名 |
| `DEPLOY_USER` | SSH 登录用户名 |
| `DEPLOY_SSH_KEY` | 服务器的 SSH 私钥（一整段） |
| `DEPLOY_PORT` | SSH 端口，默认 22，可省略 |

### 4.3 deploy.yml 关键内容（当前项目用的）

```yaml
name: Verify and deploy Flowboard

on:
  push:
    branches: [main]

concurrency:
  group: flowboard-production   # 同组任务串行，防止同时部署冲突
  cancel-in-progress: true      # 有新 push 就取消上一次还没跑完的部署

jobs:
  verify-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # 1. 验证后端：跑 Go 测试
      - uses: actions/setup-go@v5
        with:
          go-version-file: backend/go.mod
      - name: Test backend
        working-directory: backend
        run: go test ./...

      # 2. 构建前端：确认能编译通过
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Build frontend
        working-directory: frontend
        run: |
          npm ci
          npm run build

      # 3. 通过 SSH 连服务器执行部署
      - name: Deploy over SSH
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          port: ${{ secrets.DEPLOY_PORT || 22 }}   # 没配 PORT 就用 22
          script: |
            cd /opt/flowboard
            git fetch origin main
            git reset --hard origin/main   # 服务器代码强制对齐最新提交
            sh scripts/deploy.sh
```

### 4.4 scripts/deploy.sh（服务器上执行的部署）

```sh
#!/usr/bin/env sh
set -eu
# 重新构建镜像并重启两个应用服务；删除多余镜像
docker compose up -d --build --remove-orphans
docker image prune -f
```

### 4.5 使用方法

改完代码 → `git push` 到 main → 打开 GitHub 仓库 **Actions** 标签页看部署进度 → 绿勾成功 / 红叉失败可点开看日志。

---

# 第二部分：Jenkins 部署 🚧

## 1. 原理（和 GitHub Actions 对比记忆）

| | GitHub Actions | Jenkins |
|---|---|---|
| 构建跑在哪 | GitHub 云端的机器 | **你自己的服务器/机器**（这里用本机 Docker 跑） |
| 配置放哪 | 仓库里 `.github/workflows/*.yml` | 仓库里 `Jenkinsfile`（同样"配置即代码"） |
| 支持什么仓库 | 只能 GitHub | 任意（GitHub/GitLab/Gitee/自建） |
| 指定分支/项目部署 | 用 `workflow_dispatch` 输入框，能力有限 | **参数化构建**：点一下 Build with Parameters，下拉选分支、选项目 —— 这正是本次学习目标 |

**一句话理解：Jenkins 是一个常驻服务，等你说"用哪个分支、构建哪些项目、部署到哪"，它就拉代码、构建、部署。**

## 2. 安装 Jenkins（Docker 方式）

> 前提：电脑装好 Docker Desktop。本机已有 Java 21，但用 Docker 方式跑 Jenkins 时**用不到**——官方镜像里自带 JDK，容器自己跑 Java。

### 2.1 启动容器（一条命令）

```bash
docker run -d --name jenkins \
  -p 18080:8080 -p 18081:50000 \
  -v jenkins_home:/var/jenkins_home \
  --restart unless-stopped \
  jenkins/jenkins:lts-jdk21
```

参数含义：

| 参数 | 作用 |
|---|---|
| `-d` | 后台运行 |
| `--name jenkins` | 容器名，后续用 `docker stop/start/rm jenkins` 管理 |
| `-p 18080:8080` | 端口映射：本机 `18080` → 容器内 `8080`（Jenkins 默认端口） |
| `-p 18081:50000` | Jenkins 的 agent 通道端口（单机学习用不上） |
| `-v jenkins_home:/var/jenkins_home` | 数据卷：Jenkins 的配置、任务、密码都在这，**容器删了数据不丢** |
| `--restart unless-stopped` | 重启电脑后自动拉起 |

> ⚠️ **为什么不用默认的 8080？** Windows 会把一段端口（8004~8403）保留给 Hyper-V/WSL，
> 8080 正好被圈进去，直接绑定会报错：
> ```
> docker: Error response from daemon: Ports are not available: exposing port TCP 0.0.0.0:8080
> ```
> 排查方法（看系统保留了哪些端口）：
> ```bash
> netsh interface ipv4 show excludedportrange protocol=tcp
> ```
> 解决办法：换一个不在保留范围内的端口（本项目用 18080）。

### 2.2 查看初始管理员密码（两种方法）

方法一：直接读容器里的密码文件

```bash
docker exec jenkins cat /var/jenkins_home/secrets/initialAdminPassword
```

方法二：看启动日志，密码也会打印在最后

```bash
docker logs jenkins
```

> 密码是 Jenkins **首次启动时自动生成**的随机值，只用于"首次解锁"，完成初始化后就没用了。

### 2.3 浏览器初始化

1. 浏览器访问 `http://localhost:18080`
2. 粘贴上面的初始密码 → 解锁
3. 选 **安装推荐的插件**（Install suggested plugins，约 5~10 分钟，网慢可能更久）
   - 若下载卡住，可切换清华大学镜像源（见文末附录）
4. 创建第一个管理员账号（自己设账号密码，这是以后登录用的）
5. 实例配置 URL 保持默认 → Save and Finish → 出现 "Jenkins is ready"

### 2.4 日常管理命令

```bash
docker stop jenkins      # 暂停
docker start jenkins     # 重新启动
docker logs -f jenkins   # 实时看日志
docker ps                # 确认在跑
docker restart jenkins   # 重启（改配置后常用）
```

---

## 3~6 步（待学习完成后补充）

- [ ] 第 3 步：配置 GitHub 凭据，让 Jenkins 能拉取代码
- [ ] 第 4 步：改造 Jenkinsfile 支持参数化构建（选分支 + 选项目 frontend/backend/all）
- [ ] 第 5 步：新建 Pipeline 任务，Build with Parameters 跑通构建
- [ ] 第 6 步：真实部署到 /opt/flowboard 服务器

---

## 附录：Jenkins 插件镜像加速（网速慢时用）

在 Jenkins 首页 **Manage Jenkins → Plugins → Advanced settings**（或直接访问）
`http://localhost:18080/pluginManager/advanced`，把 **Update Site** 的 URL 换成：

```
https://mirrors.tuna.tsinghua.edu.cn/jenkins/updates/update-center.json
```

然后点 Check now / Submit，再装插件就快了。
